import type { QuestionSection } from "../../../../domain/questions";

export const practiceExamThreeStoreSection = {
  section: "store",
  author: "set3-store-author",
  questions: [
    {
      id: "pde-v42-p3-store-01",
      kind: "single",
      section: "store",
      objective: "3.1 Selecting storage systems: analyzing data access patterns",
      prompt: "A Spark pipeline writes an hourly output directory only after all task files are complete. Publishing the result requires atomically renaming the entire directory, including thousands of objects, and the pipeline also needs higher initial read and write throughput. The team can create a new bucket and does not require Object Versioning, Bucket Lock, object holds, or cross-bucket replication. What should it create?",
      verifiedOn: "2026-09-10",
      evidence: [{ id: "hns", title: "Hierarchical namespace", url: "https://cloud.google.com/storage/docs/hns-overview", claim: "Cloud Storage hierarchical namespace provides folders, atomic folder rename operations, and up to eight times higher initial read and write QPS; it must be enabled when a bucket is created and does not support Object Versioning, Bucket Lock, object holds, or cross-bucket replication." }],
      choices: [
        { id: "a", text: "Create a new bucket with hierarchical namespace enabled.", feedback: "Hierarchical namespace supplies the required atomic folder rename and higher initial throughput, and the scenario excludes each listed incompatible feature.", evidenceIds: ["hns"] },
        { id: "b", text: "Create a standard flat-namespace bucket and rename the output by copying every object to the final prefix.", feedback: "A flat namespace rename is implemented by copying and deleting individual objects, so it is not the required atomic directory publication operation.", evidenceIds: ["hns"] },
        { id: "c", text: "Enable Object Versioning on a standard bucket and rename the output prefix.", feedback: "Object Versioning retains versions but does not add hierarchical folders or atomic folder rename semantics.", evidenceIds: ["hns"] },
        { id: "d", text: "Use a bucket with hierarchical namespace and enable Bucket Lock for the output objects.", feedback: "Bucket Lock is not supported by hierarchical namespace buckets and is unnecessary for the stated publication workflow.", evidenceIds: ["hns"] },
      ],
      correctChoiceId: "a",
    },
    {
      id: "pde-v42-p3-store-02",
      kind: "single",
      section: "store",
      objective: "3.3 Using a data lake: managing the lake (configuring data discovery, access, and cost controls)",
      prompt: "Three domain teams share one Cloud Storage lake bucket. Each team must be able to read only objects below its own fixed prefix, while platform administrators retain bucket-wide administration. The platform needs IAM-based prefix access rather than application checks or an object ACL on every file. What should the platform configure?",
      verifiedOn: "2026-09-10",
      evidence: [{ id: "managed-folders", title: "Managed folders", url: "https://cloud.google.com/storage/docs/managed-folders", claim: "Cloud Storage managed folders are resources on which IAM roles can be granted for all objects with the managed-folder prefix; managed folders require uniform bucket-level access." }],
      choices: [
        { id: "a", text: "Enable uniform bucket-level access, create one managed folder for each prefix, and grant each team IAM roles on its folder.", feedback: "Managed-folder IAM applies to every object under the matching prefix while preserving bucket-wide administration for the platform team.", evidenceIds: ["managed-folders"] },
        { id: "b", text: "Grant each team Storage Object Viewer on the bucket and require its application to reject other prefixes.", feedback: "Bucket-level Object Viewer grants access beyond the team's prefix; application checks do not provide the required IAM boundary.", evidenceIds: ["managed-folders"] },
        { id: "c", text: "Apply an object ACL to every new file for the owning team.", feedback: "Managed folders provide prefix-scoped IAM and require uniform bucket-level access, which disables object ACLs.", evidenceIds: ["managed-folders"] },
        { id: "d", text: "Create simulated folders by adding slash characters to object names and grant IAM on the bucket.", feedback: "Simulated folders are only object-name prefixes; they do not create a resource on which prefix-scoped IAM can be granted.", evidenceIds: ["managed-folders"] },
      ],
      correctChoiceId: "a",
    },
    {
      id: "pde-v42-p3-store-03",
      kind: "single",
      section: "store",
      objective: "3.1 Selecting storage systems: planning for storage costs and performance",
      prompt: "A public research bucket is owned by a university, but independent laboratories must pay the request, retrieval, and network charges caused by their own downloads. The university must remain responsible for storage charges. Every laboratory has its own active Google Cloud billing project. What should the university enable?",
      verifiedOn: "2026-09-10",
      evidence: [{ id: "requester-pays", title: "Requester Pays", url: "https://cloud.google.com/storage/docs/requester-pays", claim: "Requester Pays requires requesters to include a billing project and bills that project for request, data processing, retrieval, and network charges, while storage and early-deletion charges remain billed to the bucket's project." }],
      choices: [
        { id: "a", text: "Enable Requester Pays and require laboratories to provide their billing project with requests.", feedback: "Requester Pays moves the specified access charges to each laboratory's billing project while leaving bucket storage charges with the university.", evidenceIds: ["requester-pays"] },
        { id: "b", text: "Grant each laboratory Storage Object Viewer and keep Requester Pays disabled.", feedback: "Object Viewer authorizes reads but does not transfer the associated access charges from the bucket project.", evidenceIds: ["requester-pays"] },
        { id: "c", text: "Place the objects in Archive storage and bill laboratories through IAM roles.", feedback: "A storage class affects storage and retrieval pricing but IAM roles do not assign access charges to a requester's billing project.", evidenceIds: ["requester-pays"] },
        { id: "d", text: "Create a separate bucket for every laboratory and make it the laboratory's owner.", feedback: "Separate buckets can change ownership but are not the managed per-request billing control requested for the shared public bucket.", evidenceIds: ["requester-pays"] },
      ],
      correctChoiceId: "a",
    },
    {
      id: "pde-v42-p3-store-04",
      kind: "single",
      section: "store",
      objective: "3.1 Selecting storage systems: lifecycle management of data",
      prompt: "A delivery service must recover any accidentally deleted or overwritten objects in a Cloud Storage bucket for 30 days. During recovery, the deleted object does not need to be readable until it is restored. The service does not need to retain every historical live version or prevent intentional deletion before a fixed retention period. What should it configure?",
      verifiedOn: "2026-09-10",
      evidence: [{ id: "soft-delete", title: "Soft delete overview", url: "https://cloud.google.com/storage/docs/soft-delete", claim: "Cloud Storage soft delete retains deleted or overwritten objects and buckets for a configured 7-to-90-day duration, during which they can be restored; soft-deleted objects cannot be read or modified before restoration." }],
      choices: [
        { id: "a", text: "Set the bucket soft delete retention duration to 30 days.", feedback: "Soft delete is the documented recovery mechanism for both accidental deletion and overwrite, and 30 days is within its supported retention range.", evidenceIds: ["soft-delete"] },
        { id: "b", text: "Enable Object Versioning and retain every noncurrent object indefinitely.", feedback: "Object Versioning retains historical live versions, but the requirement is a bounded recovery window for deleted or overwritten resources, including the bucket itself.", evidenceIds: ["soft-delete"] },
        { id: "c", text: "Set a 30-day Bucket Lock retention policy.", feedback: "A retention policy prevents deletion or replacement before it expires; the scenario instead permits deletion and requires later recovery.", evidenceIds: ["soft-delete"] },
        { id: "d", text: "Create a lifecycle rule that deletes objects after 30 days.", feedback: "A lifecycle delete action removes live objects and does not provide the required 30-day recovery state for an accidental delete or overwrite.", evidenceIds: ["soft-delete"] },
      ],
      correctChoiceId: "a",
    },
    {
      id: "pde-v42-p3-store-05",
      kind: "single",
      section: "store",
      objective: "3.1 Selecting storage systems: planning for storage costs and performance",
      prompt: "A security policy requires a Cloud Storage bucket's encryption keys to be created in the same location as the bucket, rotated under the customer's Cloud KMS policy, and auditable through Cloud KMS. Applications must continue accessing objects with normal Cloud Storage requests. Which encryption option should the team use?",
      verifiedOn: "2026-09-10",
      evidence: [{ id: "cmek", title: "Customer-managed encryption keys", url: "https://cloud.google.com/storage/docs/encryption/customer-managed-keys", claim: "Cloud Storage CMEK uses customer-controlled Cloud KMS keys, which control key rotation, access, lifecycle, and auditability; the key ring must be in the same location as the data, and the Cloud Storage service agent decrypts objects during ordinary access." }],
      choices: [
        { id: "a", text: "Configure a Cloud Storage default customer-managed encryption key from a same-location Cloud KMS key ring.", feedback: "A bucket default CMEK meets the location, customer-controlled rotation, audit, and normal application-access requirements.", evidenceIds: ["cmek"] },
        { id: "b", text: "Use Google default encryption and rotate application credentials monthly.", feedback: "Google default encryption does not give the customer control of the encryption key's rotation, lifecycle, or Cloud KMS audit trail.", evidenceIds: ["cmek"] },
        { id: "c", text: "Use a customer-managed key ring in a different region from the bucket.", feedback: "A Cloud Storage CMEK key ring must be created in the same location as the data it encrypts.", evidenceIds: ["cmek"] },
        { id: "d", text: "Require every application to send a customer-supplied encryption key with each read request.", feedback: "CMEK lets applications access objects normally because the Cloud Storage service agent decrypts them when it has key permission; per-request customer-supplied keys do not meet that operating model.", evidenceIds: ["cmek"] },
      ],
      correctChoiceId: "a",
    },
    {
      id: "pde-v42-p3-store-06",
      kind: "multiple",
      requiredSelections: 2,
      section: "store",
      objective: "3.1 Selecting storage systems: lifecycle management of data",
      prompt: "A Bigtable column family stores account status changes. Reads must return only the five newest versions whose timestamps are no more than 30 days old. The team wants asynchronous storage cleanup as well. Which two actions should it take? Choose two.",
      verifiedOn: "2026-09-10",
      evidence: [{ id: "bigtable-gc", title: "Garbage collection overview", url: "https://cloud.google.com/bigtable/docs/garbage-collection", claim: "Bigtable garbage collection is asynchronous and can combine timestamp-expiration and maximum-version rules with intersection or union; data eligible for garbage collection can remain visible until compaction, so reads must filter with the same criteria." }],
      choices: [
        { id: "a", text: "Set an intersection garbage collection policy of 30-day expiration and a maximum of five versions.", feedback: "The intersection policy marks a value for deletion only after it is both older than 30 days and outside the five newest versions, retaining at least the required recent versions while cleaning obsolete data.", evidenceIds: ["bigtable-gc"] },
        { id: "b", text: "Apply read filters that enforce the same 30-day and five-version criteria.", feedback: "Garbage collection is asynchronous, so matching read filters are required to prevent eligible-but-not-yet-compacted cells from appearing in reads.", evidenceIds: ["bigtable-gc"] },
        { id: "c", text: "Set a union garbage collection policy of 30-day expiration or a maximum of five versions.", feedback: "A union marks a cell for deletion when either rule matches, so it can remove a version merely because it is outside the newest five even when it is younger than 30 days.", evidenceIds: ["bigtable-gc"] },
        { id: "d", text: "Rely on garbage collection alone because values stop appearing as soon as they are eligible for deletion.", feedback: "Eligible values can remain visible until compaction, which can take up to a week; filtering is needed for deterministic read results.", evidenceIds: ["bigtable-gc"] },
        { id: "e", text: "Delete and recreate the column family every 30 days.", feedback: "Bigtable provides column-family garbage collection policies for version and age retention; recreating a column family would not preserve the required rolling five-version history.", evidenceIds: ["bigtable-gc"] },
      ],
      correctChoiceIds: ["a", "b"],
    },
    {
      id: "pde-v42-p3-store-07",
      kind: "multiple",
      requiredSelections: 2,
      section: "store",
      objective: "3.1 Selecting storage systems: planning for storage costs and performance",
      prompt: "A Bigtable serving application must automatically route requests to another cluster if one cluster becomes unavailable. Stale reads after a failover are acceptable. Which two actions should the team take? Choose two.",
      verifiedOn: "2026-09-10",
      evidence: [{ id: "bigtable-replication", title: "Replication overview", url: "https://cloud.google.com/bigtable/docs/replication-overview", claim: "Bigtable replication creates independent copies in multiple clusters, supports automatic failover with multi-cluster routing, and app profiles can route batch and serving requests to different clusters; replication between clusters is eventually consistent." }],
      choices: [
        { id: "a", text: "Create a replicated Bigtable instance with at least two clusters in different zones or regions.", feedback: "Replication creates independent copies in separate clusters, providing the cluster redundancy required for failover.", evidenceIds: ["bigtable-replication"] },
        { id: "b", text: "Use a multi-cluster routing app profile for the serving application.", feedback: "Multi-cluster routing automatically routes requests away from an unavailable cluster when another replicated cluster is available.", evidenceIds: ["bigtable-replication"] },
        { id: "c", text: "Use a single-cluster routing app profile for the serving application.", feedback: "Single-cluster routing targets one cluster and does not automatically route requests to a replica when that cluster is unavailable.", evidenceIds: ["bigtable-replication"] },
        { id: "d", text: "Require strong consistency between clusters before routing requests after a failover.", feedback: "Replication between Bigtable clusters is eventually consistent, and strong cross-cluster consistency is not provided by this design.", evidenceIds: ["bigtable-replication"] },
        { id: "e", text: "Create an unreplicated instance and retry failed requests against the same cluster.", feedback: "An unreplicated instance has no independent replica cluster to receive requests when its sole cluster is unavailable.", evidenceIds: ["bigtable-replication"] },
      ],
      correctChoiceIds: ["a", "b"],
    },
    {
      id: "pde-v42-p3-store-08",
      kind: "single",
      section: "store",
      objective: "3.3 Using a data lake: processing data",
      prompt: "A producer writes Parquet files to a Cloud Storage BigLake table prefix at 02:00 UTC every day. A 02:10 UTC query must include that day's files, and the team does not want BigQuery to list every object for every later query. The producer can invoke one SQL statement after its files are complete. What should the team configure?",
      verifiedOn: "2026-09-10",
      evidence: [{ id: "metadata-cache", title: "Use metadata caching for external data sources", url: "https://cloud.google.com/bigquery/docs/metadata-caching-external-tables", claim: "BigLake tables support metadata caching; manual cache mode requires a metadata refresh operation, and queries use the cached metadata subject to the configured maximum staleness interval." }],
      choices: [
        { id: "a", text: "Use manual metadata caching with a 30-minute maximum staleness interval and run the metadata refresh statement after the producer completes.", feedback: "The explicit post-write refresh makes the new files available to the 02:10 query while later queries can use the cache rather than repeatedly listing objects.", evidenceIds: ["metadata-cache"] },
        { id: "b", text: "Use automatic metadata caching and rely on its background refresh to complete by 02:10 UTC.", feedback: "Automatic refresh is asynchronous and does not provide the stated deterministic post-write freshness deadline; manual refresh does.", evidenceIds: ["metadata-cache"] },
        { id: "c", text: "Disable metadata caching so each query always lists the Cloud Storage prefix.", feedback: "Disabling the cache can discover new files but violates the requirement to avoid listing every object for later queries.", evidenceIds: ["metadata-cache"] },
        { id: "d", text: "Load every Parquet file into a native BigQuery table at 02:10 UTC.", feedback: "A native load creates a second managed copy, whereas BigLake metadata caching keeps the specified external-table storage model.", evidenceIds: ["metadata-cache"] },
      ],
      correctChoiceId: "a",
    },
    {
      id: "pde-v42-p3-store-09",
      kind: "single",
      section: "store",
      objective: "3.3 Using a data lake: processing data",
      prompt: "An insurance company stores scanned claim images in Cloud Storage. Analysts must query object URI, content type, and size in BigQuery, select qualifying images in SQL, and pass references to those same objects to a remote image-analysis function. The images must remain in Cloud Storage rather than being copied into native BigQuery columns. What should the team create?",
      verifiedOn: "2026-09-10",
      evidence: [{ id: "object-tables", title: "Introduction to object tables", url: "https://cloud.google.com/bigquery/docs/object-table-introduction", claim: "BigQuery object tables provide read-only access to Cloud Storage object metadata and ObjectRef values, allowing SQL analysis of unstructured objects and use with remote functions without loading object content into native BigQuery storage." }],
      choices: [
        { id: "a", text: "A Cloud Storage object table and a connection for the remote function.", feedback: "An object table exposes the required object metadata and references while leaving the images in Cloud Storage for remote-function processing.", evidenceIds: ["object-tables"] },
        { id: "b", text: "A native BigQuery table containing each image as a BYTES column.", feedback: "Loading image bytes into a native table creates the copy that the scenario explicitly forbids.", evidenceIds: ["object-tables"] },
        { id: "c", text: "A Cloud Storage external table with one row per image file.", feedback: "External tables are for structured external data formats; object tables are the documented table type for unstructured object metadata and ObjectRef values.", evidenceIds: ["object-tables"] },
        { id: "d", text: "A BigQuery table snapshot of a table that lists image file names.", feedback: "A snapshot preserves a point-in-time native table and does not provide Cloud Storage ObjectRef values for the selected image objects.", evidenceIds: ["object-tables"] },
      ],
      correctChoiceId: "a",
    },
    {
      id: "pde-v42-p3-store-10",
      kind: "single",
      section: "store",
      objective: "3.2 Planning for using a data warehouse: defining architecture to support data access patterns",
      prompt: "A BigQuery table is partitioned daily by event date. Analysts need the newest 400 days online, but every partition older than 400 days must be removed automatically while the table and newer partitions continue accepting data. What should the team configure?",
      verifiedOn: "2026-09-10",
      evidence: [{ id: "partition-expiration", title: "Manage partitioned tables", url: "https://cloud.google.com/bigquery/docs/managing-partitioned-tables", claim: "BigQuery partition expiration deletes individual partitions after the configured interval from their partition boundary, while table expiration deletes the entire table and all of its partitions." }],
      choices: [
        { id: "a", text: "Set the table's partition expiration to 400 days.", feedback: "Partition expiration deletes each aged daily partition while preserving the table and its more recent partitions for ongoing writes and queries.", evidenceIds: ["partition-expiration"] },
        { id: "b", text: "Set the table expiration to 400 days.", feedback: "Table expiration removes the whole table and every partition, not the rolling set of aged partitions.", evidenceIds: ["partition-expiration"] },
        { id: "c", text: "Create a lifecycle rule on a Cloud Storage bucket that deletes files after 400 days.", feedback: "This is a native BigQuery partitioned table; Cloud Storage lifecycle rules do not delete BigQuery table partitions.", evidenceIds: ["partition-expiration"] },
        { id: "d", text: "Require event-date partition filters and manually delete old partitions each week.", feedback: "A required partition filter controls query predicates but does not remove expired partitions; manual deletion is unnecessary when partition expiration is configured.", evidenceIds: ["partition-expiration"] },
      ],
      correctChoiceId: "a",
    },
  ],
} satisfies QuestionSection<"store">;
